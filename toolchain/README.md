# Toolchain

This directory contains the **precompiled STM32 toolchains** bundled with the GUI application for automated firmware building.

The toolchains allow the firmware to be compiled without requiring a local installation of STM32CubeIDE or external compiler dependencies.

---

