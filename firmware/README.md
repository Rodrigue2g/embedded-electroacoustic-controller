# Firmware

This directory contains the **embedded firmware** for the STM32-based electroacoustic controller.  
The firmware is responsible for real-time signal acquisition, control computation, and actuation.

It is designed to be built either automatically through the GUI or manually using the STM32CubeIDE.

---
